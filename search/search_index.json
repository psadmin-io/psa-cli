{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"Quickstart \u2192 Install GitHub"},{"location":"#psa-cli","title":"psa-cli","text":"<p>A unified command-line tool for PeopleSoft administration.</p> <p> </p>"},{"location":"#what-it-does","title":"What it does","text":"<p><code>psa</code> streamlines PeopleSoft administration into a single, scriptable interface. It wraps <code>psadmin</code>, the DPK installer, and Puppet apply \u2014 keeping the parts that matter, smoothing the rough edges, and producing output that's pleasant to read in a terminal and parseable as JSON when you need it.</p> <pre><code>psa domain status APPDOM            # Check a domain\npsa domain bounce APPDOM            # Stop, purge, flush, configure, start\npsa dpk stage --version 8.62.04     # Stage a DPK\npsa dpk apply --summary             # Run puppet apply, readable output\npsa discover --json                 # Find local domains, emit JSON\n</code></pre>"},{"location":"#highlights","title":"Highlights","text":"<ul> <li>Domain lifecycle \u2014 <code>list</code>, <code>status</code>, <code>start</code>, <code>stop</code>, <code>restart</code>, <code>bounce</code>, <code>compare</code> with Rich output and <code>--json</code>.</li> <li>DPK provisioning \u2014 <code>stage \u2192 setup \u2192 init \u2192 sync \u2192 apply \u2192 cleanup</code>, with a 3-tier Hiera layout and custom module install.</li> <li><code>dpk apply --summary</code> \u2014 filtered, readable Puppet output. Errors are surfaced with exit-code diagnostics.</li> <li>Discovery \u2014 find local PeopleSoft domains and optionally report them to an upstream API.</li> <li>Resilient sudo \u2014 file operations transparently escalate when the runtime user can't write.</li> </ul>"},{"location":"#status","title":"Status","text":"<p>Beta \u2014 not for production</p> <p>psa-cli is pre-1.0. The CLI surface, config schema, and command behavior may change before 1.0. Tested against PeopleTools 8.62; older versions may work but are unverified. See known limitations for caveats around JSON output and the hidden <code>ops</code> / gated <code>kit</code> command groups.</p>"},{"location":"#license","title":"License","text":"<p>MIT \u00a9 psadmin.io</p>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to psa-cli are documented in this file.</p> <p>The format is based on Keep a Changelog, and this project adheres to Semantic Versioning.</p>"},{"location":"changelog/#030-2026-05-12","title":"0.3.0 \u2014 2026-05-12","text":"<p>First public release. psa-cli is now open source under the MIT license. This release collapses the un-tagged 0.2.1 through 0.2.5 development work into the first public cut.</p> <p> Pre-1.0 / beta. Not yet recommended for production. The CLI surface, config schema, and command behavior may change before 1.0.</p>"},{"location":"changelog/#highlights","title":"Highlights","text":"<ul> <li>Domain lifecycle \u2014 <code>psa domain list / status / start / stop / restart / bounce / compare</code> with Rich-rendered output, JSON mode, quiet/verbose flags, and <code>--force</code> confirmation skips.</li> <li>DPK workflow \u2014 end-to-end PeopleSoft DPK provisioning: <code>psa dpk stage \u2192 setup \u2192 init \u2192 sync \u2192 apply \u2192 cleanup</code>, including a 3-tier Hiera layout, <code>dpk module install</code> for custom Puppet modules, <code>dpk facts</code> for site facts, and <code>dpk lookup</code> for hiera key resolution.</li> <li><code>psa dpk apply --summary</code> \u2014 filtered, readable Puppet apply output with surfaced errors, exit-code mapping, and ANSI handling.</li> <li>Discovery \u2014 <code>psa discover</code> finds local PeopleSoft domains and can report results to a configured API.</li> <li>Configuration \u2014 <code>psa config setup</code> (interactive + <code>--yes</code> non-interactive), <code>psa config show</code>, <code>psa config set</code> for individual keys, with auto-detection of <code>PS_*</code> and <code>DPK_*</code> paths.</li> <li>Resilient file operations \u2014 <code>SudoFileOps</code> abstraction transparently cascades to <code>sudo</code> when the runtime user lacks write permission, with timeout-driven diagnostics for missing passwordless sudo.</li> </ul>"},{"location":"changelog/#added-since-v020","title":"Added since v0.2.0","text":""},{"location":"changelog/#domain","title":"Domain","text":"<ul> <li><code>psa domain compare</code> replaces <code>psa domain drift</code>; archive picker, archive age display, raw-config diff that handles <code>%PS_SERVDIR%</code> vars</li> <li>Global <code>-q/--quiet</code> / <code>-v/--verbose</code> flags; Rich spinners</li> <li><code>--force/-f</code> flag (renamed from <code>--yes/-y</code>) on destructive operations</li> <li><code>psa domain status --json</code> with full process detail</li> <li>Web tier renamed <code>pia</code> \u2192 <code>web</code>; status/start/stop route through <code>psadmin -w</code> for PT 8.62</li> <li>Graceful already-stopped / already-purged handling</li> </ul>"},{"location":"changelog/#dpk","title":"DPK","text":"<ul> <li><code>psa dpk</code> command group consolidates DPK provisioning</li> <li><code>psa dpk module install &lt;org/repo&gt;</code> with <code>--branch</code>, <code>--dry-run</code>, <code>--as &lt;name&gt;</code></li> <li><code>psa dpk facts</code> (writes to <code>/etc/</code>, not the DPK install tree)</li> <li><code>psa dpk init</code> auto-creates config, generates <code>hiera.yaml</code> (eyaml backend), 3-tier hiera layout</li> <li><code>psa dpk lookup</code> for hiera key resolution</li> <li><code>psa dpk apply --summary</code> mode + puppet error surfacing + stderr scanning + Rich bypass for raw streams</li> <li><code>psa dpk apply</code> auto-sudo to root unless already root or <code>runtime_user</code></li> <li><code>psa dpk sync</code> with <code>SudoFileOps</code> permission fallback</li> <li><code>psa dpk status --json</code> + manifest parsing</li> <li><code>psa dpk cleanup --domains-only</code> for scoped domain+service teardown</li> <li><code>psa dpk stage --version</code> and <code>--dry-run</code> fix</li> <li>Split <code>DPK_BASE</code> vs <code>DPK_HOME</code> with <code>--dpk-cust-home</code> flag</li> <li>EL 9+ ncurses prereq: per-lib symlink fallback</li> <li>OL8/RHEL8+ libnsl in DPK prereq checks</li> <li>DPK repo commands: <code>init</code>, <code>status</code>, <code>list</code></li> </ul>"},{"location":"changelog/#config","title":"Config","text":"<ul> <li><code>psa config set</code> for individual config keys (<code>ps_cfg_home</code>, <code>ps_base</code>, <code>ps_home</code>, etc.)</li> <li><code>psa config show</code> groups DPK paths, uppercases path labels</li> <li><code>DPK_CUST_HOME</code> foundation (renamed from <code>PSA_CUST</code>)</li> <li><code>psa ops setup</code> moved out of <code>config</code> group (hidden by default)</li> </ul>"},{"location":"changelog/#quality-internal","title":"Quality / internal","text":"<ul> <li>Minimum Python bumped to 3.9</li> <li><code>SudoFileOps</code> centralized file operations</li> <li>Resilient domain discovery (skips permission errors, scopes by type)</li> <li>Test fixes: verbosity reset, tmadmin process-table parsing, \"Started\" prcs pattern</li> <li>Demo-lab regression fixes: status parser, PIA paths, empty errors, PS path auto-detect</li> </ul>"},{"location":"changelog/#removed","title":"Removed","text":"<ul> <li><code>psa cache</code> and <code>psa secrets</code> (stub commands that were never wired into the CLI)</li> <li><code>psa domain drift</code> (replaced by <code>psa domain compare</code>)</li> <li><code>psa ops register</code> (folded into <code>psa ops setup</code>)</li> <li><code>start/stopPIA.sh</code> shim (now routes through <code>psadmin -w</code>)</li> <li><code>io_base</code> config key</li> </ul>"},{"location":"changelog/#known-limitations","title":"Known limitations","text":"<ul> <li>Encrypted password leak in JSON \u2014 <code>psa domain --json</code> includes raw config content that contains encrypted password fields (<code>ConnectPswd</code>, <code>OprPswd</code>, etc). Values are encrypted, not cleartext, but should not appear in piped output. Tracked as issue #23, fix planned for 0.4.</li> <li><code>psa ops</code> is hidden \u2014 depends on the private psadmin.io ops API; not intended for general public use in this release.</li> <li><code>psa kit</code> is gated \u2014 requires <code>enable_psa_kit: true</code> in config and SSH access to the private <code>psadmin-io/psa-kit</code> repository.</li> <li>Tested against PeopleTools 8.62; older versions may work but are unverified.</li> <li>No PyPI release yet \u2014 install from the GitHub release wheel or from source.</li> </ul>"},{"location":"changelog/#compatibility","title":"Compatibility","text":"<ul> <li>Python 3.9, 3.10, 3.11, 3.12</li> <li>Linux (Oracle Linux 8/9, RHEL 8/9 covered; other distros likely fine)</li> <li>PeopleTools 8.62</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>Thanks for your interest in improving psa-cli.</p>"},{"location":"contributing/#reporting-bugs-requesting-features","title":"Reporting bugs / requesting features","text":"<ul> <li>File an issue: https://github.com/psadmin-io/psa-cli/issues/new/choose</li> <li>Use the Bug report template for defects and Feature request for enhancements.</li> <li>For security issues, see SECURITY.md and report privately.</li> </ul>"},{"location":"contributing/#development-setup","title":"Development setup","text":"<pre><code>git clone https://github.com/psadmin-io/psa-cli.git\ncd psa-cli\nuv sync --extra dev\n</code></pre> <p>Run tests:</p> <pre><code>uv run pytest -v\n</code></pre> <p>Lint:</p> <pre><code>uv run ruff check .\n</code></pre>"},{"location":"contributing/#branching","title":"Branching","text":"<ul> <li><code>main</code> \u2014 release branch. Tagged releases live here.</li> <li><code>staging</code> \u2014 integration branch. Feature branches PR into staging; staging is merged to main per release.</li> <li><code>feat/&lt;name&gt;</code> / <code>fix/&lt;name&gt;</code> \u2014 feature branches.</li> </ul>"},{"location":"contributing/#pull-requests","title":"Pull requests","text":"<ol> <li>Branch from <code>staging</code>.</li> <li>Run <code>pytest</code> and <code>ruff check</code> locally.</li> <li>Open a PR against <code>staging</code>. CI runs on push/PR.</li> <li>Use the PR template; reference any related issues.</li> </ol>"},{"location":"contributing/#commit-style","title":"Commit style","text":"<p>Short, concise, present-tense. Sacrifice grammar for clarity. Example:</p> <pre><code>domain compare: handle %PS_SERVDIR% in raw-config diff\n</code></pre>"},{"location":"install/","title":"Install","text":""},{"location":"install/#requirements","title":"Requirements","text":"<ul> <li>Python 3.9 or newer</li> <li>Linux (Oracle Linux 8/9, RHEL 8/9 tested; other distros likely work)</li> <li>PeopleTools 8.62 (older versions may work but are unverified)</li> </ul> <p>You can use system Python, the Python shipped with the PeopleSoft DPK, or <code>$PS_HOME/python</code>.</p>"},{"location":"install/#install-python","title":"Install Python","text":"Oracle Linux / RHELUse DPK Python <pre><code>sudo dnf install -y python3.11\n</code></pre> <p>The DPK ships with a recent Python under <code>$PS_HOME/python</code>. You can add it to your <code>PATH</code> or call it directly:</p> <pre><code>$PS_HOME/python/bin/python3 -m pip install ...\n</code></pre>"},{"location":"install/#install-psa-cli","title":"Install psa-cli","text":""},{"location":"install/#from-the-github-release","title":"From the GitHub release","text":"<p>PyPI publishing is planned. For 0.3.0, install from the GitHub release wheel:</p> <pre><code>pip install https://github.com/psadmin-io/psa-cli/releases/download/v0.3.0/psa_cli-0.3.0-py3-none-any.whl\n</code></pre>"},{"location":"install/#from-source","title":"From source","text":"<pre><code>git clone https://github.com/psadmin-io/psa-cli.git\ncd psa-cli\npip install -e .\n</code></pre>"},{"location":"install/#verify","title":"Verify","text":"<pre><code>psa --version\n</code></pre> <p>You should see <code>psa version 0.3.0</code>.</p>"},{"location":"install/#next-steps","title":"Next steps","text":"<p>\u2192 Quickstart to configure psa and run your first commands.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>This walkthrough takes you from a fresh install to a running PeopleSoft domain managed via psa. It assumes you have a PeopleSoft environment provisioned (or are about to provision one with <code>psa dpk</code>).</p>"},{"location":"quickstart/#1-configure","title":"1. Configure","text":"<p>Run the interactive setup. psa will try to auto-detect <code>PS_HOME</code>, <code>PS_CFG_HOME</code>, and the runtime user:</p> <pre><code>psa config setup\n</code></pre> <p>Or skip prompts and accept all detected defaults:</p> <pre><code>psa config setup --yes\n</code></pre> <p>Or be explicit:</p> <pre><code>psa config setup \\\n  --ps-cfg-home /u01/app/psoft/cfg \\\n  --runtime-user psadm2\n</code></pre> <p>This writes <code>~/.config/psa/config.yaml</code>. Inspect it any time:</p> <pre><code>psa config show\n</code></pre>"},{"location":"quickstart/#2-discover-domains","title":"2. Discover domains","text":"<pre><code>psa domain list\n</code></pre> <p>You should see your provisioned app, web, and process scheduler domains. To pipe into other tools:</p> <pre><code>psa domain list --json\n</code></pre>"},{"location":"quickstart/#3-check-a-domain","title":"3. Check a domain","text":"<pre><code>psa domain status APPDOM\n</code></pre> <p>This shows the running state of every server process in the domain.</p>"},{"location":"quickstart/#4-lifecycle-operations","title":"4. Lifecycle operations","text":"<pre><code>psa domain start APPDOM      # Start a stopped domain\npsa domain stop APPDOM       # Stop a running domain\npsa domain restart APPDOM    # Stop, then start\npsa domain bounce APPDOM     # Stop, purge, flush, configure, start\n</code></pre> <p>The <code>--force</code> flag (alias <code>-f</code>) skips the interactive confirmation on destructive operations.</p> <p>Run against all domains with <code>--all</code>:</p> <pre><code>psa domain stop --all\n</code></pre>"},{"location":"quickstart/#5-compare-configurations","title":"5. Compare configurations","text":"<p><code>psa domain compare</code> diffs a domain's current config against an archived <code>.cfx</code>:</p> <pre><code>psa domain compare APPDOM            # Interactive archive picker\npsa domain compare APPDOM --archive /path/to/psappsrv.cfx\n</code></pre>"},{"location":"quickstart/#6-dpk-provisioning-optional","title":"6. DPK provisioning (optional)","text":"<p>If you need to stand up a fresh environment, see the DPK provisioning workflow.</p> <p>The 10,000-foot view:</p> <pre><code>psa dpk stage --version 8.62.04     # Unpack DPK archives\npsa dpk setup                        # OS prereqs + Puppet install\npsa dpk init                         # Generate hiera.yaml + customer dir\npsa dpk sync                         # Pull YAMLs from API / source\npsa dpk apply --summary              # Run puppet apply, readable output\n</code></pre>"},{"location":"quickstart/#whats-next","title":"What's next?","text":"<ul> <li>Commands reference \u2014 every command + every flag</li> <li>DPK workflow \u2014 full PeopleSoft DPK provisioning walkthrough</li> </ul>"},{"location":"commands/","title":"Commands overview","text":"<p>psa is organized into command groups. Each group has a <code>--help</code> flag listing its subcommands.</p> Group Purpose Reference <code>psa config</code> Configure psa: paths, runtime user, defaults config <code>psa domain</code> Manage PeopleSoft domains: lifecycle, status, compare domain <code>psa dpk</code> DPK provisioning: stage, setup, init, sync, apply, cleanup dpk"},{"location":"commands/#global-flags","title":"Global flags","text":"Flag Description <code>-V</code>, <code>--version</code> Print version and exit <code>-q</code>, <code>--quiet</code> Suppress all output except errors <code>-v</code>, <code>--verbose</code> Show detailed output"},{"location":"commands/#output-modes","title":"Output modes","text":"<p>Most commands that produce structured data support <code>--json</code>:</p> <pre><code>psa domain list --json | jq '.[] | select(.type == \"app\")'\n</code></pre> <p>When stdout is a TTY, psa renders Rich tables, spinners, and color. When piped, it falls back to plain text \u2014 safe for scripts.</p>"},{"location":"commands/#configuration","title":"Configuration","text":"<p>psa reads <code>~/.config/psa/config.yaml</code>. See <code>psa config</code> for the full schema and editing commands.</p>"},{"location":"commands/config/","title":"<code>psa config</code>","text":"<p>Configure psa: paths, runtime user, defaults.</p>"},{"location":"commands/config/#config-file","title":"Config file","text":"<p>psa reads <code>~/.config/psa/config.yaml</code>. Example:</p> <pre><code>ps_cfg_home: /u01/app/psoft/cfg\nps_home: /u01/app/psoft/PT8.62\nps_base: /u01/app/psoft\nruntime_user: psadm2\ndpk_base: /u01/app/psoft\ndpk_home: /u01/app/psoft/dpk\ndpk_cust_home: /u01/app/psoft/dpk-cust\n</code></pre>"},{"location":"commands/config/#subcommands","title":"Subcommands","text":"Command Description <code>psa config setup</code> Interactive (or <code>--yes</code>) first-run configuration <code>psa config show</code> Print resolved config, with sources <code>psa config set &lt;key&gt; &lt;value&gt;</code> Set a single config key"},{"location":"commands/config/#setup-flags","title":"<code>setup</code> flags","text":"Flag Description <code>-c</code>, <code>--ps-cfg-home &lt;path&gt;</code> Override auto-detection <code>-u</code>, <code>--runtime-user &lt;name&gt;</code> Runtime user (default: <code>psadm2</code>) <code>-y</code>, <code>--yes</code> Accept all detected defaults non-interactively <p><code>setup</code> auto-detects <code>PS_HOME</code>, <code>PS_APP_HOME</code>, <code>PS_CUST_HOME</code> by <code>sudo su - &lt;runtime_user&gt;</code> and echoing the user's env. If sudo isn't configured, it falls back to interactive prompts.</p>"},{"location":"commands/config/#set-able-keys","title":"<code>set</code>-able keys","text":"<p><code>psa config set</code> accepts:</p> <ul> <li><code>ps_cfg_home</code>, <code>ps_home</code>, <code>ps_base</code></li> <li><code>dpk_base</code>, <code>dpk_home</code>, <code>dpk_cust_home</code></li> <li><code>runtime_user</code></li> <li>...and any other supported key. See <code>psa config show</code> for the full list.</li> </ul>"},{"location":"commands/config/#example","title":"Example","text":"<pre><code># First-run, accept everything\npsa config setup --yes\n\n# Then customize one value\npsa config set dpk_cust_home /u01/app/psoft/dpk-cust\n\n# Verify\npsa config show\n</code></pre>"},{"location":"commands/domain/","title":"<code>psa domain</code>","text":"<p>Manage PeopleSoft domains (app, web, process scheduler).</p>"},{"location":"commands/domain/#subcommands","title":"Subcommands","text":"Command Description <code>psa domain list</code> List all discovered domains <code>psa domain status &lt;DOMAIN&gt;</code> Show running state of every server in the domain <code>psa domain start &lt;DOMAIN&gt;</code> Start a domain <code>psa domain stop &lt;DOMAIN&gt;</code> Stop a domain <code>psa domain restart &lt;DOMAIN&gt;</code> Stop, then start <code>psa domain bounce &lt;DOMAIN&gt;</code> Stop, purge cache, flush, configure, start <code>psa domain compare &lt;DOMAIN&gt;</code> Compare current config against an archived <code>.cfx</code>"},{"location":"commands/domain/#common-flags","title":"Common flags","text":"Flag Description <code>--all</code> Operate on every discovered domain <code>-f</code>, <code>--force</code> Skip the confirmation prompt on destructive operations <code>-j</code>, <code>--json</code> Emit JSON instead of Rich tables <code>-q</code>, <code>--quiet</code> / <code>-v</code>, <code>--verbose</code> Adjust output verbosity"},{"location":"commands/domain/#examples","title":"Examples","text":"<pre><code># List all domains as JSON\npsa domain list --json\n\n# Status of all domains\npsa domain status --all\n\n# Bounce a domain without prompting\npsa domain bounce APPDOM --force\n\n# Diff current config against an archive\npsa domain compare APPDOM --archive /u01/app/.../psappsrv.cfx\n</code></pre>"},{"location":"commands/domain/#compare-archive-picker","title":"<code>compare</code> archive picker","text":"<p><code>psa domain compare &lt;DOMAIN&gt;</code> (no <code>--archive</code>) opens an interactive picker showing each <code>.cfx</code> archive in the domain's <code>archive/</code> directory with its age. Pick one to diff against the live config.</p> <p>When piped, <code>compare</code> outputs a unified diff that handles <code>%PS_SERVDIR%</code> variable substitution.</p>"},{"location":"commands/domain/#status-output","title":"Status output","text":"<p><code>psa domain status</code> recognizes the following process states from <code>psadmin</code> / <code>tmadmin</code> output:</p> <ul> <li><code>Started</code> / <code>started</code></li> <li><code>Running</code></li> <li><code>Not Started</code> / <code>not started</code></li> <li><code>Down</code></li> </ul> <p>Web tier (PIA \u2192 web rename) routes through <code>psadmin -w start</code> / <code>shutdown</code> for PeopleTools 8.62.</p>"},{"location":"commands/dpk/","title":"<code>psa dpk</code>","text":"<p>PeopleSoft DPK provisioning end-to-end: stage archives, install prereqs, generate hiera, apply Puppet.</p>"},{"location":"commands/dpk/#workflow","title":"Workflow","text":"<pre><code>stage \u2192 setup \u2192 init \u2192 sync \u2192 apply \u2192 (cleanup)\n</code></pre> <p>For a guided walkthrough see DPK provisioning workflow.</p>"},{"location":"commands/dpk/#subcommands","title":"Subcommands","text":"Command Description <code>psa dpk stage</code> Unpack DPK archives into the install dir <code>psa dpk setup</code> Run OS prereqs + Puppet install <code>psa dpk init</code> Generate <code>hiera.yaml</code>, customer dir, environment.conf <code>psa dpk sync</code> Pull YAMLs (from API or source) into the customer dir <code>psa dpk apply</code> Run <code>puppet apply</code> with the configured manifests <code>psa dpk lookup &lt;key&gt;</code> Resolve a hiera key <code>psa dpk facts</code> Write site facts to <code>/etc/facter/facts.d/</code> <code>psa dpk module install &lt;org/repo&gt;</code> Install a custom Puppet module <code>psa dpk status</code> Show install state and manifest details <code>psa dpk cleanup</code> Tear down DPK install (with <code>--domains-only</code> for scoped removal) <code>psa dpk repo init / status / list</code> Manage DPK source repositories"},{"location":"commands/dpk/#dpk-apply-summary","title":"<code>dpk apply --summary</code>","text":"<p>By default <code>puppet apply</code> floods the terminal. <code>--summary</code> mode:</p> <ul> <li>Suppresses informational Notice lines using an allow-list</li> <li>Strips ANSI escapes before prefix matching</li> <li>Surfaces Puppet errors with exit-code mapping and diagnostics</li> <li>Bypasses Rich for raw stream output so log timing is preserved</li> </ul> <pre><code>psa dpk apply --summary\n</code></pre>"},{"location":"commands/dpk/#permissions","title":"Permissions","text":"<p><code>psa dpk apply</code> auto-escalates to <code>root</code> unless already running as root or as the configured <code>runtime_user</code>.</p> <p><code>psa dpk sync</code> writes through <code>SudoFileOps</code> and transparently cascades to <code>sudo bash -c</code> when the runtime user can't write to the target.</p> <p>If a sudo write hangs more than 30s, psa assumes a password prompt and exits with a hint about configuring passwordless sudo.</p>"},{"location":"commands/dpk/#environment-variables","title":"Environment variables","text":"Variable Purpose <code>DPK_BASE</code> DPK install parent dir (used by <code>setup</code>, <code>cleanup</code>, <code>status</code>) <code>DPK_HOME</code> DPK install dir, conventionally <code>$DPK_BASE/dpk</code> (used by <code>sync</code>, <code>apply</code>, <code>init</code>) <code>DPK_CUST_HOME</code> Customer customizations dir (used by <code>init</code>, <code>module install</code>, <code>sync</code>) <p>These can also be set in <code>~/.config/psa/config.yaml</code> via <code>psa config set</code>.</p>"},{"location":"commands/dpk/#examples","title":"Examples","text":"<pre><code># Stage a specific PeopleTools version\npsa dpk stage --version 8.62.04\n\n# Initialize hiera + customer dir\npsa dpk init\n\n# Install a custom Puppet module\npsa dpk module install psadmin-io/io_role --branch develop\n\n# Resolve a hiera key\npsa dpk lookup ps_app_home\n\n# Status with JSON manifest details\npsa dpk status --json\n\n# Targeted cleanup (just one domain, all of its types)\npsa dpk cleanup --domains-only --domain APPDOM\n</code></pre>"},{"location":"dpk/provisioning-workflow/","title":"DPK provisioning workflow","text":"<p>End-to-end PeopleSoft DPK provisioning with <code>psa</code>. This guide walks through:</p> <ol> <li>Staging DPK archives</li> <li>OS prereqs and Puppet install</li> <li>Hiera + customer dir initialization</li> <li>Pulling configuration (from an API or local source)</li> <li>Running <code>puppet apply</code></li> <li>Cleanup / teardown</li> </ol>"},{"location":"dpk/provisioning-workflow/#prerequisites","title":"Prerequisites","text":"<ul> <li>A Linux host (Oracle Linux 8/9 or RHEL 8/9 recommended)</li> <li>DPK archives for your PeopleTools version (e.g. PT 8.62.04 <code>.zip</code> files)</li> <li>Passwordless <code>sudo</code> for the runtime user (recommended)</li> <li><code>psa-cli</code> installed \u2014 see Install</li> </ul>"},{"location":"dpk/provisioning-workflow/#1-stage-the-dpk","title":"1. Stage the DPK","text":"<p><code>psa dpk stage</code> unpacks DPK archives into the install directory.</p> <pre><code># Stage a specific version\npsa dpk stage --version 8.62.04\n\n# Or specify the source directory directly\npsa dpk stage --source /path/to/dpk/archives\n\n# Dry run \u2014 see what would happen, don't extract\npsa dpk stage --dry-run\n</code></pre> <p>The first stage saves <code>dpk_repo_path</code> and <code>dpk_base</code> into <code>~/.config/psa/config.yaml</code> so later commands don't need the flags.</p>"},{"location":"dpk/provisioning-workflow/#2-install-os-prereqs","title":"2. Install OS prereqs","text":"<pre><code># Check prereqs\npsa dpk setup --prereq\n\n# Install missing prereqs (uses dnf/yum under the hood)\npsa dpk setup --prereq --fix\n</code></pre> <p><code>--prereq</code> checks for required system libraries (<code>libncursesw</code>, <code>libaio</code>, <code>libnsl</code> on OL8+). On EL 9+, ncurses has a per-lib symlink fallback for compatibility.</p> <p>Run the full setup (prereqs + Puppet install + DPK relocation):</p> <pre><code>psa dpk setup\n</code></pre>"},{"location":"dpk/provisioning-workflow/#3-initialize-the-customer-dir","title":"3. Initialize the customer dir","text":"<p><code>psa dpk init</code> generates <code>hiera.yaml</code>, an empty customer directory tree, and <code>environment.conf</code>:</p> <pre><code>psa dpk init\n</code></pre> <p>The generated <code>hiera.yaml</code> uses a 3-tier layout:</p> <pre><code>common.yaml          # site-wide defaults\n&lt;tier&gt;.yaml          # DEV / TEST / PROD\n&lt;environment&gt;.yaml   # per-environment overrides\n</code></pre> <p>The eyaml backend is enabled for encrypted values. Hiera data lives in <code>$DPK_CUST_HOME/data/</code>.</p> <p>If <code>~/.config/psa/config.yaml</code> doesn't exist yet, <code>init</code> will create one.</p>"},{"location":"dpk/provisioning-workflow/#4-sync-configuration","title":"4. Sync configuration","text":"<p><code>psa dpk sync</code> pulls hiera YAMLs from your configured source into <code>$DPK_CUST_HOME</code>.</p> <pre><code># Sync from configured source\npsa dpk sync\n\n# Specify tier / environments explicitly\npsa dpk sync --tier DEV --environments FSCMDEV,HCMDEV\n\n# Specify hiera path override\npsa dpk sync --hiera-path data/custom\n</code></pre> <p>When the runtime user can't write to the target, sync transparently cascades through <code>sudo bash -c</code>. A 30-second timeout on the sudo write surfaces a hint to configure passwordless sudo.</p>"},{"location":"dpk/provisioning-workflow/#5-apply-the-manifest","title":"5. Apply the manifest","text":"<p><code>psa dpk apply</code> runs <code>puppet apply</code> against the DPK manifests.</p> <pre><code># Full apply (defaults to root via sudo when needed)\npsa dpk apply\n\n# Filtered, readable output\npsa dpk apply --summary\n</code></pre> <p><code>--summary</code> mode:</p> <ul> <li>Suppresses informational <code>Notice:</code> lines using an allow-list</li> <li>Strips ANSI escapes before prefix matching</li> <li>Surfaces Puppet errors with exit-code mapping</li> <li>Scans stderr for fatal errors</li> <li>Bypasses Rich for raw stream output so timing is preserved</li> </ul>"},{"location":"dpk/provisioning-workflow/#looking-up-resolved-hiera-values","title":"Looking up resolved hiera values","text":"<p>To check what value Puppet would see for a key without running a full apply:</p> <pre><code>psa dpk lookup ps_app_home\npsa dpk lookup ps_app_home --tier DEV\n</code></pre>"},{"location":"dpk/provisioning-workflow/#6-custom-puppet-modules","title":"6. Custom Puppet modules","text":"<p>Install a custom module from a GitHub repo:</p> <pre><code>psa dpk module install psadmin-io/io_role\npsa dpk module install psadmin-io/io_role --branch develop\npsa dpk module install psadmin-io/io_role --as my_role  # override target dir name\npsa dpk module install psadmin-io/io_role --dry-run\n</code></pre> <p>Multiple modules at once:</p> <pre><code>psa dpk module install psadmin-io/io_role psadmin-io/io_portalwar\n</code></pre>"},{"location":"dpk/provisioning-workflow/#7-facts","title":"7. Facts","text":"<p><code>psa dpk facts</code> writes site facts to <code>/etc/facter/facts.d/</code>:</p> <pre><code>psa dpk facts\n</code></pre> <p>Facts are written to <code>/etc/</code>, not the DPK install tree, so they survive DPK relocations.</p>"},{"location":"dpk/provisioning-workflow/#8-status","title":"8. Status","text":"<pre><code># Quick status\npsa dpk status\n\n# JSON, with manifest parsing\npsa dpk status --json\n</code></pre>"},{"location":"dpk/provisioning-workflow/#9-cleanup","title":"9. Cleanup","text":"<pre><code># Full teardown\npsa dpk cleanup\n\n# Just one domain (and all of its types: app, prcs, web)\npsa dpk cleanup --domains-only --domain APPDOM\n\n# Specific domain + type\npsa dpk cleanup --domains-only --domain APPDOM --type app\n</code></pre>"},{"location":"dpk/provisioning-workflow/#iteration-loop","title":"Iteration loop","text":"<p>Once provisioned, the typical edit-apply loop is:</p> <pre><code># Edit hiera files in $DPK_CUST_HOME/data/\nvim $DPK_CUST_HOME/data/common.yaml\n\n# Re-apply\npsa dpk apply --summary\n</code></pre> <p>For destructive config changes, you may want to bounce affected domains:</p> <pre><code>psa domain bounce APPDOM\n</code></pre>"},{"location":"dpk/provisioning-workflow/#related","title":"Related","text":"<ul> <li><code>psa dpk</code> reference</li> <li>DPK validation quickstart</li> </ul>"},{"location":"dpk/validation-quickstart/","title":"DPK validation quickstart","text":"<p>A fast track for validating the <code>psa dpk</code> command set against an isolated test workspace \u2014 useful for testing a new release or a development branch without touching a real environment.</p>"},{"location":"dpk/validation-quickstart/#prerequisites","title":"Prerequisites","text":"<ul> <li>DPK archive <code>.zip</code> files for your PeopleTools version</li> <li><code>psa-cli</code> installed</li> <li>Sufficient disk for an isolated install (~20 GB)</li> </ul>"},{"location":"dpk/validation-quickstart/#1-locate-dpk-archives","title":"1. Locate DPK archives","text":"<pre><code>find /nfs /media /opt /mnt -name \"PT862*.zip\" -o -name \"V*.zip\" 2&gt;/dev/null | head -5\nexport DPK_REPO=/path/to/dpk/archives\nls -lh $DPK_REPO/*.zip\n</code></pre>"},{"location":"dpk/validation-quickstart/#2-set-up-an-isolated-workspace","title":"2. Set up an isolated workspace","text":"<pre><code>export DPK_INSTALL=/tmp/dpk-validation-$(date +%s)\nexport DPK_BASE=$DPK_INSTALL/psft\n\nmkdir -p $DPK_INSTALL\n</code></pre> <p>Save these for later shells:</p> <pre><code>cat &lt;&lt;EOF &gt; /tmp/dpk-env.sh\nexport DPK_REPO=$DPK_REPO\nexport DPK_INSTALL=$DPK_INSTALL\nexport DPK_BASE=$DPK_BASE\nEOF\n</code></pre>"},{"location":"dpk/validation-quickstart/#3-stage","title":"3. Stage","text":"<pre><code>psa dpk stage --source $DPK_REPO --install-dir $DPK_INSTALL --version 8.62.04\n</code></pre> <p>Expected: archives unpack into <code>$DPK_INSTALL</code>, <code>dpk_repo_path</code> is saved to your psa config.</p>"},{"location":"dpk/validation-quickstart/#4-prereqs","title":"4. Prereqs","text":"<pre><code>psa dpk setup --prereq\n</code></pre> <p>Verify the report lists the prereq libraries with <code>\u2713</code> for installed and <code>\u2717</code> for missing. Fix any missing prereqs:</p> <pre><code>sudo dnf install -y libnsl libaio ncurses-compat-libs\npsa dpk setup --prereq\n</code></pre>"},{"location":"dpk/validation-quickstart/#5-setup","title":"5. Setup","text":"<pre><code>psa dpk setup --dpk-base $DPK_BASE\n</code></pre> <p>Expected: Puppet is installed under <code>$DPK_BASE/dpk/puppet/</code>, DPK is relocated, no fatal errors.</p>"},{"location":"dpk/validation-quickstart/#6-init","title":"6. Init","text":"<pre><code>psa dpk init --dpk-cust-home $DPK_BASE/dpk-cust\n</code></pre> <p>Verify:</p> <pre><code>ls $DPK_BASE/dpk-cust/data/\ncat $DPK_BASE/dpk/puppet/production/hiera.yaml\n</code></pre> <p>Expected: <code>data/</code> contains <code>common.yaml</code>, a tier file, and an environment file (or stubs).</p>"},{"location":"dpk/validation-quickstart/#7-apply","title":"7. Apply","text":"<pre><code>psa dpk apply --summary --dpk-home $DPK_BASE/dpk\n</code></pre> <p><code>--summary</code> mode filters Notice lines but surfaces every error. The command exits non-zero on Puppet errors and prints the matching diagnostic.</p>"},{"location":"dpk/validation-quickstart/#8-verify","title":"8. Verify","text":"<pre><code>psa dpk status --json | jq '.'\n</code></pre> <p>Expected JSON keys:</p> <ul> <li><code>dpk_base</code>, <code>dpk_home</code>, <code>dpk_cust_home</code></li> <li><code>manifest</code> \u2014 parsed manifest details</li> <li><code>puppet_version</code></li> <li><code>domains</code> \u2014 discovered after apply</li> </ul>"},{"location":"dpk/validation-quickstart/#9-cleanup","title":"9. Cleanup","text":"<pre><code>psa dpk cleanup --dpk-base $DPK_BASE --force\nrm -rf $DPK_INSTALL\n</code></pre>"},{"location":"dpk/validation-quickstart/#verifying-cli-changes","title":"Verifying CLI changes","text":"<p>For testing changes to <code>psa-cli</code> itself, install from a feature branch into an isolated venv:</p> <pre><code>python3.11 -m venv /tmp/psa-test-venv\nsource /tmp/psa-test-venv/bin/activate\npip install git+https://github.com/psadmin-io/psa-cli.git@&lt;branch&gt;\npsa --version\n</code></pre>"},{"location":"dpk/validation-quickstart/#related","title":"Related","text":"<ul> <li><code>psa dpk</code> reference</li> <li>Full provisioning workflow</li> </ul>"}]}